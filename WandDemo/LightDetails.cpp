#include "stdafx.h"

#include "LightDetails.h"
/*
void LightDetails::SetLightDirection(std::array<float, 3> direction) {
	float magnitude = -1.0f * sqrtf(direction[0] * direction[0] + direction[1] * direction[1] + direction[2] * direction[2]);
	light_direction.x = direction[0] / magnitude;
	light_direction.y = direction[1] / magnitude;
	light_direction.z = direction[2] / magnitude;
}

void LightDetails::SetLightSourceDirection(std::array<float, 3> direction) {
	float magnitude = sqrtf(direction[0] * direction[0] + direction[1] * direction[1] + direction[2] * direction[2]);
	light_direction.x = direction[0] / magnitude;
	light_direction.y = direction[1] / magnitude;
	light_direction.z = direction[2] / magnitude;
}*/